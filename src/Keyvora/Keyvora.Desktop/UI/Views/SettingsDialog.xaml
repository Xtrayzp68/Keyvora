<Window x:Class="Keyvora.Desktop.UI.Views.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:viewmodels="clr-namespace:Keyvora.Desktop.UI.ViewModels"
        Title="Settings"
        Width="420"
        Height="410"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        UseLayoutRounding="True">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="0"
                            CornerRadius="0"/>
    </shell:WindowChrome.WindowChrome>

    <Window.DataContext>
        <viewmodels:SettingsViewModel/>
    </Window.DataContext>

    <Border BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            Background="{StaticResource BackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Settings" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <CheckBox Content="Auto-connect to device on startup"
                      IsChecked="{Binding AutoConnect}"
                      Margin="0,0,0,8"
                      Foreground="{StaticResource TextPrimaryBrush}"/>
            <CheckBox Content="Auto-save profiles"
                      IsChecked="{Binding AutoSaveProfiles}"
                      Margin="0,0,0,8"
                      Foreground="{StaticResource TextPrimaryBrush}"/>
            <CheckBox Content="Enable plugins"
                      IsChecked="{Binding EnablePlugins}"
                      Margin="0,0,0,8"
                      Foreground="{StaticResource TextPrimaryBrush}"/>
            <CheckBox Content="Start minimized to tray"
                      IsChecked="{Binding StartMinimized}"
                      Margin="0,0,0,8"
                      Foreground="{StaticResource TextPrimaryBrush}"/>
            <CheckBox Content="Launch at Windows startup"
                      IsChecked="{Binding LaunchAtStartup}"
                      Margin="0,0,0,8"
                      Foreground="{StaticResource TextPrimaryBrush}"/>

            <Grid Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="COM Port:" VerticalAlignment="Center"
                           Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,12,0"/>
                <Grid Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0"
                              ItemsSource="{Binding AvailablePorts}"
                              SelectedItem="{Binding SelectedPort}"
                              Background="{StaticResource SurfaceLightBrush}"
                              Foreground="{StaticResource TextPrimaryBrush}"/>
                    <Button Grid.Column="1"
                            Style="{StaticResource SecondaryButton}"
                            Content="⟳"
                            FontSize="12"
                            Padding="4,2"
                            Margin="4,0,0,0"
                            Command="{Binding RefreshPortsCommand}"
                            ToolTip="Refresh COM ports"/>
                </Grid>
            </Grid>

            <Grid Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Baud Rate:" VerticalAlignment="Center"
                           Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,12,0"/>
                <ComboBox Grid.Column="1" SelectedItem="{Binding BaudRate}"
                          Background="{StaticResource SurfaceLightBrush}"
                          Foreground="{StaticResource TextPrimaryBrush}">
                    <ComboBoxItem Content="9600"/>
                    <ComboBoxItem Content="19200"/>
                    <ComboBoxItem Content="38400"/>
                    <ComboBoxItem Content="57600"/>
                    <ComboBoxItem Content="115200" IsSelected="True"/>
                </ComboBox>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}"
                    Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Save" Style="{StaticResource PrimaryButton}"
                    IsDefault="True" Click="OnSaveClick"/>
        </StackPanel>
    </Grid>
    </Border>
</Window>
