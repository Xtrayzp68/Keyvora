<Window x:Class="Keyvora.Desktop.UI.Views.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Renommer le profil"
        Width="380"
        Height="170"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        UseLayoutRounding="True">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="0"
                            CornerRadius="0"/>
    </shell:WindowChrome.WindowChrome>

    <Border BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Nouveau nom du profil :"
                   FontSize="14"
                   Margin="0,0,0,10"/>

        <TextBox x:Name="NameTextBox"
                 Grid.Row="1"
                 FontSize="14"
                 Padding="10,8"
                 KeyDown="OnTextBoxKeyDown"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Annuler" Style="{StaticResource SecondaryButton}"
                    Padding="14,6" Margin="0,0,10,0" IsCancel="True"/>
            <Button Content="OK" Style="{StaticResource PrimaryButton}"
                    Padding="14,6" IsDefault="True" Click="OnOkClick"/>
        </StackPanel>
    </Grid>
    </Border>
</Window>
